ALTER PROCEDURE $procedureName
AS
CREATE TABLE [core_schema].$entity.getTableName()(
    #foreach($column in $columns.entrySet())
        #if($foreach.index != $columns.size() - 1)
            $column.key $column.value.concat(',')
        #else
            $column.key $column.value
        #end
    #end
);

